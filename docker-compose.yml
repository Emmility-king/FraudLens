version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    volumes:
      - ./backend/app:/app/app:rw
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
