# Copy to .env and adjust (never commit .env)

# SQLite (async SQLAlchemy — school / local default)
# Relative path is resolved from the backend process cwd (often repo root).
DATABASE_URL=sqlite+aiosqlite:///./data/fineguard.db

# API
API_V1_PREFIX=/api/v1
